namespace RPGSandBox.Controller
{
    public class AICommand : PlayerCommandSystem
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
